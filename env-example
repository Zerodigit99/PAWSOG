# Session ID for authentication
SESSION_ID=your_session_id_here

# Optional configurations
REQUEST_TIMEOUT=10          # Adjust as needed
RETRY_DELAY=5              # Adjust as needed
MAX_RETRIES=3              # Adjust as needed

ENABLE_QUESTS=True         # Set to True or False based on your needs
QUEST_CHECK_INTERVAL=60    # Interval for checking quests in seconds
DELAY_BETWEEN_QUESTS=10    # Delay between quest executions in seconds

SERVER_CHECK_INTERVAL=30    # Interval for server checks
SERVER_CHECK_TIMEOUT=5      # Timeout for server checks
MAX_SERVER_CHECK_ATTEMPTS=3 # Max attempts for server checks
SERVER_CHECK_RETRY_DELAY=2  # Delay between server check retries
SLEEP_ON_SERVER_ERROR=True   # Set to True to sleep on server error
SLEEP_AFTER_SESSIONS=10     # Sleep time after handling sessions

# Logging configurations
LOGGING_LEVEL=INFO          # Set your preferred logging level
ENABLE_RICH_LOGGING=False    # Set to True for rich logging if needed
DETAILED_LOGGING=False       # Set to True for detailed logging
LOG_AUTH_DATA=False          # Set to True to log authentication data
LOG_RESPONSE_DATA=False      # Set to True to log response data
LOG_REQUEST_DATA=False       # Set to True to log request data
LOG_USER_AGENT=              # Optionally set a custom user agent
LOG_PROXY=False              # Set to True to log proxy information